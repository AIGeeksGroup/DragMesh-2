# Environment Config

`configs/env/hand_config.yaml` controls the Isaac Gym simulator, SMPL-X hand asset, GAPartNet asset root, camera options, drag parameters, and output paths.

The default paths are repository-relative:

```yaml
hand.hand_asset_root: hands/floating
asset.asset_root: assets
save.root: output/hand_drag
```
