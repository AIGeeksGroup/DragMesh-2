# GLA Training Configs

These configs use `params.network.name: gla_actor_critic`, registered by `backbones/gla/a2c_network.py`.

- `ppo.yaml`: GLA backbone baseline.
- `ppo_pool_mean.yaml`: GLA baseline using mean temporal pooling instead of last-token pooling.
