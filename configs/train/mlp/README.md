# MLP Training Configs

These configs use the built-in rl-games `actor_critic` network.

- `ppo.yaml`: plain PPO baseline.
- `pica.yaml`: flat MLP with PICA physical regularization.
- `pica_drand12.yaml`: flat MLP with PICA physical regularization and damping randomization.
