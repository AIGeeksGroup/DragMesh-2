# Assets

Place external articulated-object assets here. The default config expects GAPartNet-style assets:

```text
assets/
`-- gapartnet_example/
    `-- <object_id>/
        |-- mobility_annotation_gapartnet.urdf
        |-- mobility_v2.json
        `-- link_annotation_gapartnet.json
```

Large mesh and dataset files are ignored by git.
