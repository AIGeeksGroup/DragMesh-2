# MLP Backbone

The MLP baseline uses the built-in rl-games `actor_critic` network. There is no custom Python builder for this backbone.

Configs:

- `configs/train/mlp/ppo.yaml`
- `configs/train/mlp/pica.yaml`
- `configs/train/mlp/pica_drand12.yaml`
