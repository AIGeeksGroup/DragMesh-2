# Transformer Backbone

This directory is reserved for the Transformer temporal-backbone ablation described in the paper.

No custom Transformer builder is included in this release. Do not select a Transformer config unless the implementation is added and registered in `ppo/train.py`.
