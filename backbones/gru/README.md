# GRU Backbone

This directory is reserved for the GRU temporal-backbone ablation described in the paper.

No custom GRU builder is included in this release. Do not select a GRU config unless the implementation is added and registered in `ppo/train.py`.
